/*
 * Intellinet Beratung und Technologie GmbH
 */

package de.intellinet.ausbildung.zahlenausgabe;

/**
 * [TODO Insert description here.]
 * 
 * @author ahmalk
 *
 * @since 25.09.2019
 */
public class Serves {

    /*
     * static String Eingabe = "99";
     * static String zahlAusgabe0_19[] =
     * {"Null", "Eins", "Zwei", "Drei", "Vier", "Fünf", "Sechs", "Sieben", "Acht", "Neun", "zehn",
     * "zwölf",
     * "dreizehn", "vierzehn", "fünfzehn", "sechzehn", "siebzehn", "achtzehn", "neunzehn"};
     * static String zahlAusgabe0_9[] = {"", "Ein", "Zwei", "Drei", "Vier", "Fünf", "Sechs", "Sieben",
     * "Acht", "Neun"};
     * static String zahlAusgabe20_90[] =
     * {"", "", "Zweizig", "Dreizig", "Vierzig", "Fünfzig", "Sechszig", "Siebzig", "Achtzig",
     * "Neunzig"};
     * static String Ergebnis;
     * public static int l = Eingabe.length();
     * if(l==1)
     * {
     * int position = Integer.parseInt(Eingabe);
     * Ergebnis = zahlAusgabe0_19[position];
     * }else
     * {
     * int position = Integer.parseInt(Eingabe);
     * if (position <= 11 || position <= 19) {
     * Ergebnis = zahlAusgabe0_19[position - 1];
     * } else {
     * for (int i = l - 1; i >= 0; i--) {
     * int c = Eingabe.charAt(i);
     * int position2 = Character.getNumericValue(c);
     * if (i == 1) {
     * if (position2 == 0) {
     * } else
     * System.out.print(zahlAusgabe0_9[position2] + "und");
     * } else {
     * System.out.print(zahlAusgabe20_90[position2]);
     * }
     * }
     * }
     * }
     */

}
